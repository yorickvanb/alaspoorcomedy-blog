{# {%- css %}.postlist { counter-reset: start-from {{ (postslistCounter or postslist.length) + 1 }} }{% endcss %}
<ol reversed class="postlist">
  {% for post in postslist | reverse %}
      <li class="postlist-item{% if post.url == url %} postlist-item-active{% endif %}">
        <a href="{{ post.url }}" class="postlist-link">{% if post.data.title %}{{ post.data.title }}{% else %}<code>{{ post.url }}</code>{% endif %}</a>
        {% if post.data.description %}
              <div class="postlist-description">{{ post.data.description | trim | safe }}</div>
        {% endif %}
        <time class="postlist-date" datetime="{{ post.date | htmlDateString }}">{{ post.date | readableDate("LLLL yyyy") }}</time>
      </li>
  {% endfor %}
</ol> #}



<div class="cards">
  {% for post in postslist | reverse %}
    {# {{post.url}} #}
    <div class="card">

      <a href="{{ post.url }}" class="postlist-link">
        {% if post.data.title %}{{ post.data.title }}
        {% else %}
          <code>{{ post.url }}</code>
        {% endif %}
      </a>

      {# {% if post.data.youtubelink %}
        <div class="postlist-video">
          <iframe src="{{post.data.youtubelink}}" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" allowfullscreen></iframe>
        </div>
      {% endif %} #}

      {% if post.data.youtube_video_id %}
      <div class="postlist-video-div">
        <img class="postlist-video-image" src="https://img.youtube.com/vi/{{post.data.youtube_video_id}}/hqdefault.jpg">
        <img class="postlist-video-playbtn" src="/_includes/assets/youtube_play_button.svg">
      </div>
      {% endif %}



      {% if post.data.description %}
            <div class="postlist-description">{{ post.data.description | trim | safe }}</div>
      {% endif %}

      <time class="postlist-date" datetime="{{ post.date | htmlDateString }}">{{ post.date | readableDate("LLLL yyyy") }}</time>
    </div>
    {% endfor %}
</div>
